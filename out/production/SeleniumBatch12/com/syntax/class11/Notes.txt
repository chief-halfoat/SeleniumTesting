Selenium Class 11

Actions:
If regular click does not work, you can use the alternative way of having Actions class click method

Upload documents:
To upload docs using selenium, we simply do sendKeys(filePath)

JavaScriptExecutor
    library in Selenium which can be executed in different programming languages
    (java, c#, python, etc)
    JSEx is an interface
    Actions which can be performed by using JSEx
        click on the element
        scroll through the page
        refresh
        highlight an element
        scroll to a specific element
        open a new tab